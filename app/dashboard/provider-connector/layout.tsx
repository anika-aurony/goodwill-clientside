export const metadata = {
  title: "Goodwill | Provider Connector Dashboard",
};

export default function ProviderConnectorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
